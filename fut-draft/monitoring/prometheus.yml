global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "fut-draft-backend"
    scheme: https
    metrics_path: /api/metrics
    static_configs:
      - targets: ["squaddraft.ru"]
